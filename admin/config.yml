backend:
  name: git-gateway
  branch: main # Branch to update (optional; defaults to master)
media_folder: 'assets/uploads'
collections:
  - name: 'courses'
    label: 'courses'
    folder: '_courses/'
    fields:
      - { name: Title }
